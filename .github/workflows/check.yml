name: check

on:
  push:
  pull_request:

jobs:
  shell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update
      - run: sudo apt-get install -y shellcheck jq git
      - run: TOKENHUD_REQUIRE_SHELLCHECK=1 ./scripts/check.sh
